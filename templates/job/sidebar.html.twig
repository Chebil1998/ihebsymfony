 {% block sidebar %}

<ul class="nav flex-column">
{% for item in listjobs %}

  <li class="nav-item">
  {%if (item.nomjob=='Developpeur web') %}
    <a class="nav-link active" href="{{item.id}}" style="color:red">{{item.nomjob}}</a>
    {% else %}
        <a class="nav-link active" href="{{item.id}}" >{{item.nomjob}}</a>

  </li>
  {% end if %}
  {% endfor %}

</ul>

{% end block %}